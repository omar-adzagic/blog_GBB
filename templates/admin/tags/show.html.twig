{% extends 'admin/admin_base.html.twig' %}

{% block title %}{{ 'edit_tag'|trans_session }}{% endblock %}

{% block bread %}
    → <a href="{{ path('admin_tag_index') }}">
    {{ 'tags'|trans_session }}
</a> → {{ 'show'|trans_session }}
{% endblock %}

{% block body %}
    <div class="card" style="width: 33%; margin-left: auto; margin-right: auto; margin-top: 10%">
        <h4 class="card-title my-3 mx-auto">{{ 'tag_details'|trans_session }}</h4>
        <div class="card-body rounded bg-light border">
            <p class="card-text">
                <strong>{{ 'tag_name'|trans_session }}:</strong> {{ tag.name }}
            </p>
        </div>
    </div>
{% endblock %}
