{% extends 'admin/admin_base.html.twig' %}

{% block title %}
    {{ 'add_tag'|trans({}, null, app.session.get('_locale', 'en')) }}
{% endblock %}

{% block bread %}
    → <a href="{{ path('admin_tag_index') }}">
    {{ 'tags'|trans({}, null, app.session.get('_locale', 'en')) }}
</a> → {{ 'add'|trans({}, null, app.session.get('_locale', 'en')) }}
{% endblock %}

{% block body %}
    {{ include('admin/tags/_form.html.twig') }}
{% endblock %}
