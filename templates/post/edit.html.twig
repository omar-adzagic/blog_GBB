{% extends 'base.html.twig' %}

{% block title %}
    {{ 'editing_post'|trans({}, null, app.session.get('_locale', 'en')) }}
{% endblock %}

{% block bread %}
    → <a href="{{ path('app_post_show', {slug: post.slug}) }}">
    {{ 'post'|trans({}, null, app.session.get('_locale', 'en')) }}
</a> → {{ 'edit'|trans({}, null, app.session.get('_locale', 'en')) }}
{% endblock %}

{% block body %}
    {{ include ('post/_form.html.twig') }}
{% endblock %}
