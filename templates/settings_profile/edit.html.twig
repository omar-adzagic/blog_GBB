{% extends 'base.html.twig' %}

{% block title %}
    {{ 'profile_edit'|trans_session|title }}
{% endblock %}

{% block bread %}
    → <a href="{{ path('app_profile') }}" class="text-decoration-none">
    {{ 'the_profile'|trans_session|title }}
</a> → {{ 'actions.edit'|trans_session|title }}
{% endblock %}

{% block body %}

{{ form_start(form) }}
{{ include('settings_profile/_profile_form.html.twig', {readonly: false}) }}
<div class="row">
    <div class="col-12 col-md-6 mt-2">
        <button type="submit" class="btn btn-primary btn-block shadow-sm">
            {{ 'actions.save'|trans_session|title }}
        </button>
    </div>
</div>
{{ form_end(form) }}

{% endblock %}